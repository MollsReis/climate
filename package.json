{
  "name": "climate",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://evilscott.github.io/climate",
  "devDependencies": {
    "react-scripts": "0.8.5"
  },
  "dependencies": {
    "leaflet": "^1.0.3",
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "redux": "^3.6.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build&&gh-pages -d build"
  }
}
